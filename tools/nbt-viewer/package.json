{
  "name": "redstone-nbt-viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "node scripts/build-wasm.mjs",
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build",
    "prepare:mcmeta": "node scripts/prepare-mcmeta-from-vscode-cache.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "deepslate": "^0.25.1",
    "gl-matrix": "^3.4.4"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.11"
  }
}
